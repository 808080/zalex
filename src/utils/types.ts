export type ID = `${number}`;

export type Status = 'New' | 'Under Review' | 'Pending' | 'Done';

export type IssueDate = `${number}/${number}/${number}`;

export type Option = { value: string, label: string };